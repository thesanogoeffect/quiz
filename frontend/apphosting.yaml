runConfig:
  runtime: nodejs20
  
env:
  - variable: NUXT_APP_BASE_URL
    value: /ipt/
    
build:
  commands:
    - yarn install --frozen-lockfile
    - yarn build:ipt
  
start:
  command: yarn start